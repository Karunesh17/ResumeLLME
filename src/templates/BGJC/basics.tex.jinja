%==== Profile ====%
\begin{tabular*}{7in}{l@{\extracolsep{\fill}}r}
	\textbf{\Large \VAR{name}} & \textit{\BLOCK{ if address } \VAR{address} | \BLOCK{ endif }
  \BLOCK{ if email } \href{mailto:\VAR{email}}{\VAR{email}} | \BLOCK{ endif }
  \BLOCK{ if phone } \href{tel:\VAR{phone|replace(' ', '')|replace('-', '')|replace('(', '')|replace(')', '')}}{\VAR{phone}} | \BLOCK{ endif }
  \BLOCK{ if website } \href{\BLOCK{ if website.startswith('http') }\VAR{website}\BLOCK{ else }https://\VAR{website}\BLOCK{ endif }}{\VAR{website}} \BLOCK{ endif }\ }
\end{tabular*}